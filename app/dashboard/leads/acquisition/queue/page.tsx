import Acquisition from '../Acquisition'

/** MY QUEUE — what waits on me across every prospect (the blueprint, §10; acq-queue-core.ts; 22 Sep 2026) */
export default function Page() {
  return <Acquisition view="queue" />
}
