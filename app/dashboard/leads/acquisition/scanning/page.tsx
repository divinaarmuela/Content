import Acquisition from '../Acquisition'

/** SCANNING — what the inboxes and the agent read, and what came of it (acq-scanning-core.ts; 22 Sep 2026) */
export default function Page() {
  return <Acquisition view="scanning" />
}
