'use client'

import ScannerSettings from '../ScannerSettings'
import SuperOnly from '../SuperOnly'

export default function ScannerSettingsPage() {
  return <SuperOnly><ScannerSettings /></SuperOnly>
}
