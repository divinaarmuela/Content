'use client'

import PageAccessSettings from '../PageAccessSettings'
import SuperOnly from '../SuperOnly'

export default function PageAccessPage() {
  return <SuperOnly><PageAccessSettings /></SuperOnly>
}
