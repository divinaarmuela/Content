'use client'

import IntakeTemplates from '../IntakeTemplates'
import SuperOnly from '../SuperOnly'

export default function IntakeTemplatesPage() {
  return <SuperOnly><IntakeTemplates /></SuperOnly>
}
