/**
 * A designer's card, at its own address (the owner, 22 Sep 2026: "the designer
 * page, when a card is clicked, took me to editor"). The card page is the
 * editor's, shared; it reads its own address to know which board to go back
 * to and what to call it.
 */
export { default } from '../../editor/[id]/page'
