import AvailabilityView from '../AvailabilityView'

/** Google-calendar availability for shoot planning, as its own route. */
export default function SchedulerAvailabilityPage() {
  return <AvailabilityView />
}
